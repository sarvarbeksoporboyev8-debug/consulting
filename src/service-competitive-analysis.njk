---js
{
  pagination: { data: "languages", size: 1, alias: "lang" },
  permalink: function(data) { return data.lang.code + "/service-competitive-analysis.html"; },
  layout: "layouts/page.njk",
  pageCss: "css/service-competitive-analysis.css",
  navKey: "Services",
  extraFonts: "&family=Merriweather:wght@700",
  currentPage: "service-competitive-analysis.html",
  serviceIdx: 0,
  eleventyComputed: {
    title: function(data) { var L = data.lang.code; return (data.serviceDetails[0][L] || data.serviceDetails[0].en).title; },
    description: function(data) { var L = data.lang.code; return (data.serviceDetails[0][L] || data.serviceDetails[0].en).description; },
    heroTitle: function(data) { var L = data.lang.code; return (data.serviceDetails[0][L] || data.serviceDetails[0].en).heroTitle; },
    breadcrumbs: function(data) {
      var L = data.lang.code;
      return [
        { label: (data.ui[L]||data.ui.en).page_services_hero, url: "services.html" },
        { label: (data.serviceDetails[0][L] || data.serviceDetails[0].en).breadcrumbLabel }
      ];
    }
  }
}
---
{% include "partials/service-detail-content.njk" %}
